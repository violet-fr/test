# P3 办公协同模块 — 验收测试报告

| 项目 | 内容 |
|---|---|
| 项目名称 | 智慧园区管理平台 |
| 验收模块 | P3 办公协同模块(小组二) |
| 子模块 | ① 通知公告(已读/未读统计) ② 会议室预约(时间冲突检测) ③ 审批流程(多级流转状态机) |
| 技术栈 | FastAPI + SQLAlchemy + Alembic + PostgreSQL 16(实际仓库实现,非培训计划中的 Spring Boot) |
| 验收日期 | 2026-09-09 |
| 验收方式 | Swagger UI(`http://localhost:8000/docs`)黑盒接口测试 + 后端日志/数据库结果核对 |
| 测试账号 | admin(超管)、employee01(普通员工) |
| 接口总数 | 24 个(公告 6 + 会议室 8 + 审批 10) |

---

## 一、验收环境

| 项 | 说明 |
|---|---|
| 数据库 | PostgreSQL 16,库名 `smart_park`,用户 `park`,端口 5432 |
| Python | 64 位 Python 3.11(`py -3.11`) |
| 数据库迁移 | Alembic 0001 → 0004 全部执行,24 张表已建 |
| 后端服务 | uvicorn 运行于 `http://0.0.0.0:8000`,API 文档 `/docs` |
| 鉴权方式 | JWT Bearer Token(Swagger 右上角 Authorize 全局授权,有效期 120 分钟) |
| 初始数据 | admin / dev_admin_pass_change_me;employee01 / emp123456 |

---

## 二、功能验收结果总览

| 子模块 | 核心功能点 | 用例数 | 通过 | 结论 |
|---|---|---|---|---|
| 通知公告 | 发布、列表、详情自动标已读、已读/未读统计 | 4 | 4 | ✅ 通过 |
| 会议室预约 | 会议室创建、预约、**时间冲突检测**(重叠拦截/相邻放行) | 4 | 4 | ✅ 通过 |
| 审批流程 | 模板配置、发起申请、待办、**状态机**(通过/驳回/撤回)、流转记录 | 8 | 8 | ✅ 通过 |
| **合计** | | **16** | **16** | **✅ 全部通过** |

验收过程中发现并修复缺陷 1 个(见第五节),修复后回归通过。

---

## 三、详细测试用例

### 3.1 通知公告模块(接口前缀 `/api/v1/oa/notices`)

| 编号 | 接口 | 测试方法 | 预期 | 实际结果 | 结论 |
|---|---|---|---|---|---|
| N-01 | `POST /notices` | admin 发布公告"国庆节放假通知" | code=0,返回公告 id | 返回公告 id=2,发布成功 | ✅ |
| N-02 | `GET /notices` | 查询公告列表 | 列表含已发布公告 | total=1,含新发布公告 | ✅ |
| N-03 | `GET /notices/{id}` | 查看公告详情 | 返回详情;**当前用户自动写入已读记录** | 详情正常,查看后该用户已读标记生成 | ✅ |
| N-04 | `GET /notices/{id}/read-stat` | 查询已读统计 | 返回总数/已读数/未读数/未读人列表 | total=2,read_count=1,unread_count=1,未读人=employee01 | ✅ |

**验证要点**:已读/未读统计口径正确——admin 查看详情后自动记为已读,employee01 未查看记为未读,统计数字与未读人员名单完全吻合。

### 3.2 会议室预约模块(接口前缀 `/api/v1/oa`)

| 编号 | 接口 | 测试方法 | 预期 | 实际结果 | 结论 |
|---|---|---|---|---|---|
| M-01 | `POST /meeting-rooms` | 创建会议室:第一会议室/A栋3楼/20人/投影仪·白板·视频会议 | code=0,返回会议室 | 返回 id=1,创建成功 | ✅ |
| M-02 | `POST /bookings` | 预约①:room_id=1,项目周会,2026-09-10 **09:00–10:00** | 预约成功 | code=0,booking id=1,status=1 | ✅ |
| M-03 | `POST /bookings` | 预约②:room_id=1,客户接待,**09:30–10:30**(与①重叠) | **拒绝,提示冲突** | code=**20034**,msg="该时段已被预约(2026-09-10 09:00:00 ~ 2026-09-10 10:00:00)" | ✅ |
| M-04 | `POST /bookings` | 预约③:room_id=1,部门培训,**10:00–11:00**(与①首尾相邻) | 预约成功(半开区间边界不冲突) | code=0,booking id=2 | ✅ |

**冲突检测算法验证**:

- 重叠判定条件:`newStart < existEnd AND newEnd > existStart`(半开区间 `[start, end)`)
- 用例 M-03:09:30 < 10:00 且 10:30 > 09:00 → 判定重叠,拦截并返回冲突时段明细 ✅
- 用例 M-04:10:00 开始 = 上一场 10:00 结束,区间不重叠 → 放行 ✅
- 结论:**重叠必拦截、相邻可预约**,边界判断正确,且错误信息能明确指出与哪条预约冲突。

### 3.3 审批流程模块(接口前缀 `/api/v1/oa`)

**测试数据**:审批模板 id=1「请假审批流程」(type=leave),节点配置 `[{order:1, role_id:1, name:"主管审批"}]`(单级审批,审批人=admin)。

**实例状态枚举**:0=待审批,1=审批中,2=已通过,3=已驳回,4=已撤回
**流转动作枚举**:1=通过,2=驳回,3=撤回

| 编号 | 接口 | 测试方法 | 预期 | 实际结果 | 结论 |
|---|---|---|---|---|---|
| A-01 | `POST /approval-templates` | 创建模板,node_config 单节点 order=1/role_id=1 | code=0,模板创建 | 返回模板 id=1 | ✅ |
| A-02 | `POST /approvals` | 发起申请 id=1「我的请假申请」(请假2天) | code=0,status=0,current_node=1;自动生成首节点待办 | id=1,status=0,node=1 | ✅ |
| A-03 | `GET /approvals/todo` | admin 查询待我审批 | 返回 id=1 的待办任务 | 待办列表含 instance_id=1 | ✅(修复后,见第五节) |
| A-04 | `POST /approvals/1/approve` | 审批通过,comment="同意,注意工作交接" | 末节点通过 → status=2 | code=0;详情查得 **status=2**,流转记录 action=1 | ✅ |
| A-05 | `POST /approvals` | 发起申请 id=2「测试驳回的申请」 | status=0 | id=2,status=0 | ✅ |
| A-06 | `POST /approvals/2/reject` | 驳回,comment="近期项目忙,不予批准" | 实例终止 → status=3 | code=0;详情查得 **status=3**,流转记录 action=2 | ✅ |
| A-07 | `POST /approvals` | 发起申请 id=3「测试撤回的申请」 | status=0 | id=3,status=0 | ✅ |
| A-08 | `POST /approvals/3/withdraw` | 申请人撤回(无 body) | 仅申请人可撤 → status=4 | code=0;详情查得 **status=4**,流转记录 action=3("申请人撤回") | ✅ |

**状态机终态核对**(`GET /approvals/my` 最终结果):

| 申请 id | 标题 | status | 含义 | 流转记录 |
|---|---|---|---|---|
| 1 | 我的请假申请 | **2** | 已通过 | action=1 通过,审批人 admin,意见"同意,注意工作交接" |
| 2 | 测试驳回的申请 | **3** | 已驳回 | action=2 驳回,审批人 admin,意见"近期项目忙,不予批准" |
| 3 | 测试撤回的申请 | **4** | 已撤回 | action=3 撤回,操作人=申请人本人,"申请人撤回" |

**验证要点**:

- 发起申请(status=0)后系统按首节点角色自动匹配审批人并生成待办任务;
- 末节点"通过"→ 实例 PASSED(2);任一节点"驳回"→ 实例 REJECTED(3)直接终止;申请人"撤回"→ WITHDRAWN(4);
- 每次操作均写入流转记录(节点、审批人、动作、意见、时间),`GET /approvals/{id}` 可查全链路;
- 多级流转逻辑(中间节点通过 → current_node+1 并生成下一节点待办,status=1 审批中)代码已实现,本次单级模板覆盖末节点分支。

---

## 四、接口交付清单(共 24 个)

**通知公告 `/api/v1/oa/notices`(6 个)**

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/notices` | 公告列表(分页) |
| POST | `/notices` | 发布公告 |
| GET | `/notices/{id}` | 公告详情(自动标已读) |
| PUT | `/notices/{id}` | 修改公告 |
| DELETE | `/notices/{id}` | 删除公告 |
| GET | `/notices/{id}/read-stat` | 已读/未读统计 |

**会议室预约 `/api/v1/oa`(8 个)**

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/meeting-rooms` | 会议室列表 |
| POST | `/meeting-rooms` | 新建会议室 |
| PUT | `/meeting-rooms/{id}` | 修改会议室 |
| DELETE | `/meeting-rooms/{id}` | 删除会议室 |
| GET | `/meeting-rooms/{id}/bookings` | 某会议室预约列表 |
| GET | `/bookings` | 预约列表 |
| POST | `/bookings` | 预约会议室(含冲突检测) |
| DELETE | `/bookings/{id}` | 取消预约 |

**审批流程 `/api/v1/oa`(10 个)**

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/approval-templates` | 模板列表 |
| POST | `/approval-templates` | 新建模板(节点 order 连续校验) |
| PUT | `/approval-templates/{id}` | 修改模板 |
| POST | `/approvals` | 发起申请(自动建首节点待办) |
| GET | `/approvals/my` | 我的申请 |
| GET | `/approvals/todo` | 待我审批 |
| GET | `/approvals/{id}` | 审批详情(含流转记录) |
| POST | `/approvals/{id}/approve` | 审批通过(末节点→PASSED,否则推进下一节点) |
| POST | `/approvals/{id}/reject` | 审批驳回(→REJECTED) |
| POST | `/approvals/{id}/withdraw` | 撤回申请(仅申请人,→WITHDRAWN) |

---

## 五、缺陷修复记录

| 缺陷编号 | BUG-001 |
|---|---|
| 现象 | `GET /api/v1/oa/approvals/todo` 请求返回 **422 Validation Error**,"待我审批"接口不可用 |
| 根因 | FastAPI 按注册顺序匹配路由。原代码中 `GET /approvals/todo` 注册在 `GET /approvals/{instance_id}` **之后**,请求 `/todo` 时被动态路由 `{instance_id}` 抢先匹配,字符串 `"todo"` 无法转换为 int → 422 |
| 修复 | 将 `/approvals/todo` 路由定义移动到 `/approvals/{instance_id}` **之前**,并添加注释说明静态路由必须先于动态参数路由注册 |
| 涉及文件 | `app/api/v1/oa/approvals.py`(list_todo 函数位置调整) |
| 回归验证 | 修复并重启后端后,`GET /approvals/todo` 正常返回待办列表(A-03 通过) |

---

## 六、交付物清单

**代码(test 仓库)**

- 模型:`app/models/notice.py`、`app/models/notice_read.py`(新增)、`app/models/meeting.py`、`app/models/approval.py`
- Schema:`app/schemas/notice.py`、`app/schemas/meeting.py`、`app/schemas/approval.py`
- 路由:`app/api/v1/oa/` 下 `notices.py`、`meetings.py`、`approvals.py`、`__init__.py`(已在 `app/api/v1/__init__.py` 挂载 oa_router)
- 迁移:`alembic/versions/0002_add_meeting_room.py`、`0003_add_notice_read.py`、`0004_add_approval.py`
- 配套改动:`0001_init.py`(pgvector 扩展自动检测,无 pgvector 时 face_feature 降级为 Text)、`app/core/dependencies.py`(鉴权改用 HTTPBearer,支持 Swagger 全局 Authorize)

**数据库**

- 24 张表全部建好;初始账号 admin / employee01 可登录;P3 测试数据(会议室 1 间、预约 2 条、审批模板 1 个、审批实例 3 条)已留存,可直接用于演示。

---

## 七、验收结论

P3 办公协同模块三个子模块的**核心功能点全部验证通过**:

1. **通知公告**:发布/查看/已读自动标记/已读未读统计,统计口径准确;
2. **会议室预约**:时间冲突检测算法正确,重叠时段拦截(错误码 20034 并提示冲突明细)、相邻时段放行,边界条件符合半开区间语义;
3. **审批流程**:多级流转状态机运转正常,待审批(0)→审批中(1)→已通过(2)/已驳回(3)/已撤回(4) 状态流转与流转记录均正确,待办按角色自动分配。

验收过程发现 1 个路由顺序缺陷,已修复并回归通过。

**验收结论:✅ 通过,可进入交付/答辩环节。**

> 备注:本模块不含前端(按要求仅交付后端 API);代码尚未提交 git,待确认后统一提交。
